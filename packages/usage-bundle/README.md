# @dsh-usage-meter/usage-bundle

可安装的 profile bundle：一条 `cordis.patch.yml` 同时挂载 `usage-host`（记录器）与 `usage-ui`（仪表盘）两个插件行。发布到 npm 后通过 `dsh plugin --profile web add @dsh-usage-meter/usage-bundle` 安装；未发布时见根 README「方式二」。
