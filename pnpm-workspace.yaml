# Single-package project. pnpm 11 reads allowBuilds from this file, not from
# package.json; the "packages" list keeps it a valid workspace root so esbuild
# can run its postinstall.
packages:
  - '.'

allowBuilds:
  esbuild: true
